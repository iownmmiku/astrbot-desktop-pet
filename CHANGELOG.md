# 更新日志

## [0.6.2] - 2026-09-04

### 修复

- **拖动异常缩放**：拖动结束后强制重新验证 Canvas 尺寸和模型缩放，防止 DPI/显示器变化导致模型变大。
- **resize 事件误触发**：只在窗口内容区尺寸真实变化时重新计算缩放，避免拖动时误触发布局重算。


## [0.6.1] - 2026-09-04

### 修复

- **WebSocket 重连内存泄漏**：清理旧连接和定时器，防止指数级重连和内存泄漏。
- **Panel fetch 超时控制**：所有 HTTP 请求增加 10 秒超时，避免网络挂起导致 UI 冻结。
- **多显示器拖动边界**：使用目标位置所在显示器边界，支持跨显示器拖动和负坐标。

### 优化

- **TTS 消息队列**：语音播放改为队列模式，连续消息不再丢失，依次播放。
- **模型加载状态**：加载期间连接点显示"正在加载模型…"，完成后恢复连接状态。
- **面板可见性感知**：面板隐藏时暂停状态自动刷新，恢复可见时立即刷新。
- **右键菜单定位**：缓存菜单尺寸避免每次布局计算，消除边缘闪烁。
- **配置常量提取**：性能参数集中管理（位置同步频率、超时、分辨率等），便于调优。


## [0.6.0] - 2026-09-03

### 优化

- 统一鼠标穿透状态机：菜单/聊天/说话框/状态面板打开或鼠标在模型区域时接收点击，其余透明区域穿透。
- 漫游移动改为绝对坐标定位，消除 moveBy 取整累积漂移。
- 托盘新增「重新加载模型」「重置位置」。
- 模型加载/完成增加气泡提示；模型路径失败时回退默认模型并提示。
- 状态面板新增经验条、连接信息与更新时间。
- 右键菜单新增「随机动作」「重置位置」。
- 互动接口在 WS 断开时直接显示回复；操作失败有气泡提示。
- 插件 API 增加版本号与连接数信息。

## [0.5.9] - 2026-09-03

### 修复

- 固定 Pixi 渲染分辨率为 CSS 像素，避免拖动窗口时 DPI 变化导致模型及界面整体变大。
- 拖动期间禁止 Canvas 尺寸和模型缩放重算，仅在真实窗口尺寸变化时重新布局。



发布新版本前，必须先在本文件新增对应版本条目。

## [0.5.8] - 2026-09-03

### 修复

- 恢复控制面板原有 UI 显示大小，页面缩放限制只作用于桌宠窗口。
- 修复右键菜单顶部项目因固定菜单高度和透明区域穿透导致无法点击的问题。
- 右键菜单改用实际渲染尺寸定位，并在打开期间强制保持鼠标交互。

## [0.5.7] - 2026-09-03

### 修复

- 锁定 Chromium 页面与 Electron 内容区缩放，修复拖动桌宠时整体异常变大的问题。

## [0.5.6] - 2026-09-03

### 修复

- 修复模型重复缩放导致异常变大的问题。